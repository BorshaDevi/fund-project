
import WarpLayout from '@/compent/layout'
import './globals.css'
import QueryProvider from '@/compent/query-provider'
export const metadata = {
  title: 'Next.js',
  description: 'Generated by Next.js',
}


export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        <QueryProvider>
        <WarpLayout>
        {children}
        </WarpLayout>
        </QueryProvider>
        </body>
    </html>
  )
}
